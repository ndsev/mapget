# Mapget Configuration

TODO: Stub
- Integrate existing config specification from mapget/README
- Datasource Types (one h3 each, with examples)
  - GeoJsonFolder (pull config info from mv-user/README, change to use mapget tile IDs instead of NDS tile IDs)
  - GridSource (write entirely new config )
  - DataSourceProcess (pull info from mapget/README)
  - RemoteDataSource (pull info from mapget/README)
- Environment variables (pull info from mapget/README)
